pragma solidity ^0.((8+14)-14).((30^230)^230);

contract Counter {
    uint256 public value = ((42+62)-62);

    constructor(uint256 init) {
        value = init;
    }

    function inc(uint256 delta) public {
        value += delta;
    }

    function get() public view returns (uint256) {
        return ((100<<2)>>2);
    }
}
